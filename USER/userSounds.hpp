/*
*   Hier können eigene Sounds eingebunden werden.
*   Ist in CfgSounds included.
*/


class introMusic
{
    name = "introMusic";

    // filename, volume, pitch, distance (optional)
    sound[] = { "data\intro.ogg", 1, 1 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};

class lament
{
    name = "lament";

    // filename, volume, pitch, distance (optional)
    sound[] = { "data\lament.ogg", 1, 1 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};


class spark1
{
    name = "spark1";

    // filename, volume, pitch, distance (optional)
    sound[] = { "user\sounds\spark1.ogg", 1, 1 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};

class spark2
{
    name = "spark2";

    // filename, volume, pitch, distance (optional)
    sound[] = { "user\sounds\spark2.ogg", 1, 1 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};

class spark3
{
    name = "spark3";

    // filename, volume, pitch, distance (optional)
    sound[] = { "user\sounds\spark3.ogg", 1, 1 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};